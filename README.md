# 🎨 Jaspion Design Studio

Visual page annotation and design collaboration tool.

## Quick Start
```bash
bash serve.sh
# Open http://localhost:3847
```

## Features
- **Page Preview** — Load any URL with device viewport toggles
- **Annotations** — Click to place, drag to reposition, leader lines to target
- **Chat** — Built-in chat with "Send to Jaspion" export
- **Image Upload** — Drag & drop reference images
- **Export** — Annotations as JSON, report as markdown

## Keyboard Shortcuts
- `Escape` — Close modals
- `Enter` — Send chat message

## Notes
- Cross-origin iframes may be blocked; use screenshot mode for external sites
- All state persists in localStorage
